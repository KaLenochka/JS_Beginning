const hello = 'Hello'
const world = 'World'
const greeting = hello + ' ' + world
//шаблонные строки
const greeting2 = `${hello} ${world}`
console.log(greeting)
console.log(greeting2)

const name = 'Lena'
const city = 'Wroclaw'
const hi = `My name is ${name} and I live in ${city}`

console.log(hi)